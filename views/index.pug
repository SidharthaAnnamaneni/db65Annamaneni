//- extends layout

//- block content
//-   h1= title
//-   p This is Lab Assignment using pug templates to dynamically generate web pages on the server side.  
//-   button(type='button',onclick='update()',class='btn btn-primary') Press Me
  
//-   div(id="target") text of Display 

//-   script.
//-     var client_count = 0;
//-     function update(){
//-       client_count +=1;
//-       document.getElementById("target").innerHTML = "Value now " + client_count;
//-     }

extends layout
block content
 if (!user)
   a(href="/login") Login
   br
   a(href="/register") Register
 if (user)
   p You are currently logged in as #{user.username}
   a(href="/kites") Take me to kites
   br
   a(href="/logout") Logout